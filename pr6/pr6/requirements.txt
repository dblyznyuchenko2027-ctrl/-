# ПР6. RAG-помічник за власною базою знань

# Клієнт OpenAI-сумісного API — той самий, що в ПР3–ПР4
openai>=1.40

# Локальна модель ембедінгів — та сама, що в ПР5. Разом із пакетом
# ставиться torch; для цієї роботи достатньо версії для CPU
sentence-transformers>=3.0

# Вектори, схожість, збереження індексу
numpy>=1.26

# Пошук за ключовими словами. Потрібен, лише якщо ваш відбір фрагментів
# використовує його поруч із семантичним; інакше рядок можна прибрати
rank_bm25>=0.2

# Читання налаштувань із .env
python-dotenv>=1.0

# Веб-рівень. pydantic приходить разом із FastAPI — ним можна описати
# і схему відповіді моделі
fastapi>=0.115
uvicorn[standard]>=0.30

# Перевірка за JSON Schema, написаною вручну. Якщо схему описуєте
# pydantic-моделлю, цей рядок можна прибрати
jsonschema>=4.20

# Потрібен, лише якщо сторінку рендеритимете шаблоном.
# Спосіб віддати сторінку — на ваш вибір, тож рядок можна прибрати.
jinja2>=3.1
